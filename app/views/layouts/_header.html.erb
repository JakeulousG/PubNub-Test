<% if user_signed_in? %>
    <div class="d-flex justify-content-between mb-3 bg-info p-3">
        <h5 class='text-white'>Welcome, <%= current_user.name %>!</h5>
        <%= link_to "Edit User", edit_user_registration_path, class:'btn btn-primary text-white' %>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class:'btn btn-danger' %>
    </div>
<% end %>